package aula16;

public class OperadorTernario {
    void main() {

        // Variável
        double nota = 8;

        // Condicional
        String situacao = nota >= 7 ? "Aprovado" : "Reprovado";
        System.out.println(situacao);

        // Explicando:
        // A nota é maior ou igual a 7, caso sim, então ( ? ) "Aprovado". Se não ( : ) "Reprovado:.
        //

    }

}
